
[project]
name = "micropython-tsdb"
version = "0.0.1"
dependencies = [
    "mpflash",
    "esptool",
    "mpremote",
    "jonnor-micropython",
]

[project.optional-dependencies]
dev = [
    "pandas",
    "pyarrow",
]

[tool.setuptools]
packages = []

